apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nameOverride | default "nodeapp" }}
spec:
  type: NodePort
  selector:
    app: nodeapp
  ports:
    - port: 80
      targetPort: 3000
      nodePort: {{ .Values.nodePort }}
